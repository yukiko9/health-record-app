<view class="screen enter-ui">
  <view class="page-bg-layer page-bg-breathe-enter" style="{{pageBgStyle}}" />
  <view class="binder-decor-layer" aria-hidden="true" />
  <view class="bubble-layer">
    <view class="foam-bubble foam-a"></view>
    <view class="foam-bubble foam-b"></view>
    <view class="foam-bubble foam-c"></view>
    <view class="foam-bubble foam-d"></view>
    <view class="foam-bubble foam-e"></view>
    <view class="foam-bubble foam-f"></view>
    <view class="foam-bubble foam-g"></view>
    <view class="foam-bubble foam-h"></view>
    <view class="foam-bubble foam-i"></view>
    <view class="foam-bubble foam-j"></view>
    <view class="foam-bubble foam-k"></view>
    <view class="foam-bubble foam-l"></view>
    <view class="foam-bubble foam-m"></view>
    <view class="foam-bubble foam-n"></view>
    <view class="foam-bubble foam-o"></view>
    <view class="foam-bubble foam-p"></view>
    <view class="foam-bubble foam-q"></view>
    <view class="foam-bubble foam-r"></view>
  </view>
  <view class="enter-ui-foreground">
  <floating-bubbles />

  <view class="enter-main" bindtap="enterMainUi">
    <view class="goal-tip">
      <text>目标:</text>
      <text>\n</text>
      <text class="goal">{{goal}}</text>
      <text>\n</text>
      <text class="situation">{{situation}}</text>
    </view>

    <view class="mood-nudge-wrap">
      <text class="mood-emoji icon-emoji">{{moodEmoji}}</text>
    </view>
    <view class="score-bump-wrap {{scoreBump ? 'score-entry-bump' : ''}}">
      <view class="score-nudge-hint">
        <rolling-score score-class="score label-strong" value="{{score}}" />
      </view>
    </view>
    <view class="username-row">
      <view class="username label-strong">{{username}}</view>
      <view class="sync-nickname-btn card pressable" catchtap="syncWechatNickname" hover-class="pressing">同步微信昵称</view>
    </view>
    <view class="float-tip">点击空白处进入</view>
  </view>

  <view class="enter-fab-stack" catchtap="noopReadme">
    <view class="feedback-fab-col" catchtap="noopReadme">
      <view id="feedback-btn" class="feedback-btn card pressable" catchtap="openFeedback" hover-class="pressing">✍️</view>
      <text class="feedback-fab-caption">写反馈❤️</text>
    </view>
    <view class="readme-fab-col" catchtap="noopReadme">
      <view class="readme-btn card pressable" catchtap="openReadme" hover-class="pressing">?</view>
      <text class="readme-fab-caption">用户说明</text>
    </view>
  </view>

  <view wx:if="{{feedbackVisible}}" class="feedback-mask" catchtap="closeFeedback">
    <view class="feedback-panel card" catchtap="noopFeedback">
      <view class="feedback-close pressable" catchtap="closeFeedback" hover-class="pressing">×</view>
      <scroll-view scroll-y class="feedback-scroll">
        <textarea
          class="feedback-textarea"
          value="{{feedbackText}}"
          placeholder="写下你的体验、改进建议，这对我来说很重要！❤️"
          placeholder-class="feedback-placeholder"
          maxlength="2000"
          bindinput="onFeedbackInput"
          show-confirm-bar="{{false}}"
        />
        <view class="workflow-grading">
          <view
            class="workflow-grade-row"
            wx:for="{{workflowGradeUi}}"
            wx:for-item="row"
            wx:key="key"
          >
            <text class="workflow-grade-label">{{row.label}}</text>
            <view class="workflow-stars">
              <text
                wx:for="{{workflowStarSlots}}"
                wx:for-item="n"
                wx:key="*this"
                class="workflow-star {{row.value >= n ? 'workflow-star-on' : ''}}"
                data-row-key="{{row.key}}"
                data-star="{{n}}"
                catchtap="onWorkflowStarTap"
              >{{row.value >= n ? '★' : '☆'}}</text>
            </view>
          </view>
        </view>
        <scroll-view scroll-y class="feedback-tag-scroll">
          <view class="feedback-tag-area">
            <view
              wx:for="{{feedbackTags}}"
              wx:key="*this"
              class="feedback-tag {{feedbackSelected[item] ? 'feedback-tag-selected' : ''}}"
              data-tag="{{item}}"
              catchtap="toggleFeedbackTag"
            >{{item}}</view>
          </view>
        </scroll-view>
        <view class="feedback-submit btn-primary pressable" catchtap="submitFeedbackForm" hover-class="pressing">提交</view>
      </scroll-view>
    </view>
  </view>

  <view wx:if="{{readmeVisible}}" class="readme-mask" catchtap="closeReadme">
    <view class="readme-page card readme-pop-animate" catchtap="noopReadme">
      <view class="readme-power-emoji">💪</view>
      <view class="readme-close pressable" catchtap="closeReadme" hover-class="pressing">×</view>
      <scroll-view scroll-y class="readme-scroll">
        <text class="readme-placeholder" selectable>
          😆小绿笔记本为常居室内的学生、上班族等朋友们提供日常活动状态记录并用健康分值让朋友们更好地了解每日的健康状况。一起来设立你的健康目标并完成每日打卡吧！\n\n小绿的主要职责是帮助你记录日常的简单健康活动记录，主界面的下面三个模块分别对应了三大日常活动模块！点进去有使用的附带说明噢！\n\n小绿在活动记录之外，也有四个提升朋友们的体验的功能：连续打卡记录、个人定制目标设置、从你的健康app界面截图读取睡眠和热量消耗数据、显示你最近的活动、提供可点击的高频重复活动记录帮助你快速进行记录。\n\nTip:使用截图读取功能时，请尽量提供一张完整包含睡眠数据和热量消耗数据的截图噢！\n\n出于体验版限制，目前同步微信昵称功能无法使用！
        </text>
      </scroll-view>
    </view>
  </view>
  </view>
</view>
